import { FC } from 'react';
import { Login as LoginComponent } from '../../components/index';

const Login: FC = () => {
  return (
    <div className="py-8">
      <LoginComponent />
    </div>
  );
};

export default Login;
